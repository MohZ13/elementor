Usage:
    $0 [url] [options]

Options:
  --url           URL to open
  --nonAngular    Launch protractor with "browser.ignoreSynchronization = true"
  --configPath    Path to protractor configuration
  -h, --help      Print usage information
  -v, --version   Show version
